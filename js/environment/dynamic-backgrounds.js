// Dynamic Backgrounds (v2)
// Nota: este arquivo é um ponto de extensão. Hoje o fundo é renderizado em GameV2._renderBackground.
// Próximo passo: extrair fundos por estética para cá e adicionar camadas extras (rios, vilarejos, etc.).

window.SuperBario99 = window.SuperBario99 || {};

(function () {
  SuperBario99.dynamicBackgrounds = {
    // placeholder
  };
})();
